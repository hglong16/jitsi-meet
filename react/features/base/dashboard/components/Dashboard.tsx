import React from 'react';
import { Layout } from '../../layouts/dashboard/Layout';

const Dashboard = () => {
  return (
    <Layout>
      <div>Dashboard</div>
    </Layout>
  );
};

export default Dashboard;